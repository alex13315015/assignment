server:
  port: 8090

spring:
  output:
    ansi:
      enabled: always
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
  mail:
    host: smtp.naver.com
    port: 465
    username: alex7006@naver.com
    password: DBVVUX7YTSTG
    properties:
      debug: true
      smtp:
        auth: true
        ssl:
          enable: true
          trust : smtp.naver.com
    protocol: smtps


  thymeleaf:
    cache: false

  #### database ??
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521:xe
    username: springryan
    password: ryan

#### mybatis ??
mybatis:
  config-location: classpath:mapper/config/config.xml
  mapper-locations: classpath:mapper/sql/**/*.xml
  type-aliases-package: com.ryan9025.board.dto  # comma

#### log ??  fatal > error > warn > info > debug
logging:
  level:
    root: info
    '[com.ryan9025.board]': info

file:
  path: C:/upload/